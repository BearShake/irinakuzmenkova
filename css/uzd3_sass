$colwhite: #ffffff;
$colblack: #000000;
$spacing: 7px;
$font1: "Barlow Condensed", sans-serid;
$background: #9f2720;
$btncolor: #323232;
$btnfontcol: #fff;
$bord: 0;
$def-font: 14px;
$noborder :0px;


@mixin col-options($px){
  padding-top: 7px;
  padding-bottom: 7px;
}

@mixin same-col($col){
  background-color: $col;
  border-color: $col;
}

.msg-title-header, .title, .description, .resultTitle, .result, .code {
  .btn {
    @include col-options($spacing);
    font-family: "Barlow Condensed", sans-serif;
  }
}


.msg-title-header, .title, .description, .resultTitle, .result, .code {
  .btn {
    @extend .msg-title-header .btn;
  }
}


  .navbar-inverse {
    background-color: $btncolor;
    border-style: none;
    border-radius: $noborder;


    .navbar-nav{
      @extend .navbar-nav > li > a:focus;
    }

    .navbar-nav > li > a{
      color: $colwhite;
      font-family: $font1;
    }

    .navbar-nav > li > a:hover{
      color: $colwhite;
      background-color: $background;
    }

    .navbar-nav > li > a:focus {
      @extend .navbar-nav > li > a:hover;
    }

    .navbar-nav > .active > a{
      @extend .navbar-nav > li > a:hover;
    }

    .navbar-nav > .active > a:hover{
      @extend .navbar-nav > li > a:hover;
    }

    .navbar-nav > .active > a:focus {
      @extend .navbar-nav > li > a:hover;
    }

    .navbar-toggle{
      @include same-col($background);
    }

    .navbar-toggle .icon-bar {
      background-color: $colwhite;
    }

    .navbar-toggle:hover{
      @extend .navbar-inverse .navbar-toggle;
    }

    .navbar-toggle:focus{
      @extend .navbar-inverse .navbar-toggle;
    }
  }


  .navbar-brand{
    padding: $spacing + 3;
  }

  .navbar-brand img{
    max-width: ($spacing + 3) * 10;
  }

  .title {
    font-size: $spacing * 2;
    letter-spacing: $spacing * 2 - 2;
    text-align: center;
    background-color: $btncolor;
    color: $colwhite;
  }

  .description{
    @extend .navbar-inverse .navbar-nav > li > a:hover;
  }

  .resultTitle{
    @extend .description;
    background-color: $btncolor;
  }

  .result{
    background-color: $colwhite;
    color: $colblack;
  }


  .code{
    .btn{
      background-color: $btncolor;
    }
  }

  .btn-warning{
    background-color: $btncolor;
    border: $noborder;
    border-radius: $noborder;
    font-family: $font1;
  }

  .btn-warning:hover{
    color: $btnfontcol;
    background-color: $btncolor;
    border: $noborder;
  }

  .btn-warning:focus{
    @extend .btn-warning:hover;
  }

  .btn-warning:focus:active{
    @extend .btn-warning:hover;
  }

  body{
    margin-top: 80px;
  }

  .row{
    margin-left: $noborder;
    margin-right: $noborder;
  }

  p{
    margin: $noborder $noborder $noborder;
  }

  .code{
    background-color: $background;
  }

  .hideCode{
    display: none;
  }

  .showCode{
    display: block;
  }

